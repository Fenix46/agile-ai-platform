
import { LucideProps } from "lucide-react";
import { google, eye, eyeOff, user, logIn, mail } from "lucide-react";

export const Icons = {
  google,
  eye,
  eyeOff,
  user,
  logIn,
  mail
} as const;
